parted:
  args:
    type: list
  default:
    - device: /dev/sdb
      number: 1

mkfs:
  args:
    type: list
  default:
    - device: /dev/sdb1
      fstype: ext4

nfs_pkgs:
  args:
    type: list
  default:
    - nfs-utils
